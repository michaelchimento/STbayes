% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LFO_helpers.R
\name{STb_lfo}
\alias{STb_lfo}
\title{STb_lfo()}
\usage{
STb_lfo(
  lfo_type = c("parallel", "sequential"),
  fit,
  STb_data,
  stan_model,
  M,
  L,
  k_thres = 0.7,
  ...
)
}
\arguments{
\item{lfo_type}{String specifying how future observations are left out.
\code{"parallel"} predicts the next observations within each trial.
\code{"sequential"} moves through trials sequentially.}

\item{fit}{A fitted \code{STbayes} model object to use as the initial reference
fit.}

\item{STb_data}{A formatted \code{STbayes} data object returned by \code{\link[=import_user_STb]{import_user_STb()}}.}

\item{stan_model}{Character string containing the Stan model code returned by
\code{\link[=generate_STb_model]{generate_STb_model()}}.}

\item{M}{Integer of future observations to predict at each step.}

\item{L}{Integer of observations to condition on before the
first prediction/refit. This should be larger than the number of demonstrators.}

\item{k_thres}{Numeric. Pareto-\eqn{k} threshold above which the model is
refit. Defaults to \code{0.7}.}

\item{...}{Additional arguments passed to the model refitting step.}
}
\value{
An object with attributes:
\describe{
\item{\code{pointwise}}{A data frame containing pointwise \code{elpd_lfo} and
\code{pareto_k} values.}
\item{\code{estimates}}{A named numeric vector containing summaries \code{elpd_lfo},
\code{se_elpd_lfo}, \code{lfoic}, and \code{se_lfoic}.}
\item{\code{refits}}{Observation indices at which the model was refit.}
}
}
\description{
Estimate leave-future-out expected log predictive density for an STbayes model.
}
\details{
The function refits the model at selected points and uses Pareto-smoothed
importance sampling between refits to approximate M-step-ahead predictive
performance. Cross-validation can be done either within trials in parallel or
through the full event sequence.
}
